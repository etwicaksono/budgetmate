/**
 * @module types
 *
 * Shared cross-domain type definitions.
 * Domain-specific types (User, Transaction, Debt, etc.) live inside their
 * respective feature modules under src/features/<domain>/types/.
 */
export * from './api.types';
export * from './filter.types';
export * from './backup.types';
