/**
 * @module features/sync
 * Canonical import: import { syncService } from '@/features/sync';
 * Note: Backend sync logic lives in @/lib/sync (server-only).
 */
export * from '@/services/currencyFormatService';
